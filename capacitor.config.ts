import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'matrix_marketers',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
